\documentclass[cameraready]{Interspeech}
\usepackage{float}
\usepackage{amsmath}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{array}
\usepackage{multirow}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{stfloats}

\title{Unsupervised Outlier Detection: Performance, Robustness, and Calibration}

\author[affiliation={1}]{Paweł}{Pozorski}

\address{
    $^1$ Warsaw University of Technology, Poland
}

\email{pawel.pozorski.stud@pw.edu.pl}

\keywords{Anomaly detection, unsupervised learning, outlier detection, robustness analysis, hyperparameter sensitivity, benchmark evaluation}

\begin{document}

\maketitle

\begin{abstract}
This report evaluates six unsupervised outlier detection methods---One-Class SVM, Isolation Forest, Local Outlier Factor, DBSCAN, ECOD, and HBOS---on a heterogeneous corpus of 37 datasets (ODDS tabular benchmarks plus selected 2D geometric datasets). The study compares methods under a unified preprocessing and evaluation protocol, with labels reserved strictly for evaluation. Beyond point performance, it analyzes seed stability, hyperparameter sensitivity, method redundancy, and contamination-calibration behavior.

Results show no universal winner. Within the engineered/non-PCA-capable family, Isolation Forest has the strongest aggregate profile, while within the PCA-95 local family DBSCAN exceeds LOF on thresholded metrics. Variance decomposition suggests that cross-dataset heterogeneity dominates seed-level noise. Correlation and dimensional structure remain relevant stressors, and rank-correlation analysis confirms partial redundancy alongside useful complementarity between local and global methods. Elbow-based contamination estimates are informative as initialization but show systematic underestimation on high-contamination datasets. A final verification task on unlabeled \texttt{test\_data.csv} uses a rank-based ensemble to produce binary labels.
\end{abstract}

\section{Introduction}

Outlier detection is routinely deployed in settings where anomaly labels are scarce or unavailable at training time. In these conditions, method selection is difficult: detector performance often depends not only on nominal metric quality but also on data geometry, contamination regime, and sensitivity to hyperparameters. Consequently, practical use requires more than reporting a single leaderboard.

This project investigates unsupervised anomaly detection under a broad, intentionally heterogeneous benchmark. The goals are fourfold: (i) compare core detector families under a shared protocol, (ii) identify hyperparameters that materially affect performance, (iii) analyze robustness and failure modes across dataset characteristics, and (iv) validate a deployable strategy for labeling an unlabeled test set.

The report combines quantitative benchmarking with qualitative geometric diagnostics on 2D datasets. It also includes method-redundancy and contamination-calibration analyses to support practical decisions about ensembling and thresholding. The remainder of the paper is structured as follows: Section~\ref{sec:datasets} describes the corpus and preprocessing, Section~\ref{sec:experiments} details the experimental design, Section~\ref{sec:evaluation} defines metrics and robustness reporting, Section~\ref{sec:results} presents empirical findings, and Section~\ref{sec:discussion} summarizes practical implications.

\section{Models}

The detector suite is designed to probe complementary failure modes rather than to optimize a single benchmark score. It spans boundary learning, random isolation, local density contrast, density-connectivity clustering, and marginal distribution scoring. This diversity supports mechanism-level interpretation under a common preprocessing and evaluation protocol.

\begin{description}
    \item[OCSVM] OCSVM is the boundary-based baseline. With an RBF kernel, it learns a flexible boundary around nominal observations in feature space. The hyperparameter \texttt{nu} controls boundary strictness through the upper bound on training errors/outlier fraction. Points outside the boundary are classified as anomalous \cite{scholkopf2001support}.

    \item[IForest] IForest is the partition-based baseline. It isolates points through random recursive splits; sparse and atypical points are separated with shorter path lengths than dense nominal regions. The average isolation depth is therefore used as an anomaly signal \cite{liu2008isolation}.

    \item[LOF] LOF captures local anomaly structure by comparing each point's local density to that of its \(k\)-nearest neighbors. Points with substantially lower local density than their neighborhood receive higher outlier scores, making LOF sensitive to local deviations in heterogeneous-density data \cite{breunig2000lof}.

    \item[DBSCAN] DBSCAN separates dense connected regions from noise via \(\epsilon\)-neighborhood density connectivity. Points not reachable from any dense component are labeled as noise. In this setting, DBSCAN provides binary outlier assignments rather than continuous anomaly scores \cite{ester1996dbscan}; ROC-AUC and PR-AUC are therefore degenerate single-point areas rather than full threshold-swept curves.

    \item[ECOD] ECOD is the marginal-distribution baseline. It estimates empirical tail extremeness per feature through CDF-based ranks and aggregates this evidence across dimensions. Because it avoids parametric distribution assumptions and uses few hyperparameters, it serves as an efficient reference model \cite{li2022ecod}.

    \item[HBOS] HBOS also uses feature-wise marginal evidence, but via discretized histograms instead of continuous CDF tails. Observations that repeatedly fall into low-density bins receive higher anomaly scores. The method is computationally light, with performance governed mainly by binning choices \cite{goldstein2012hbos}.
\end{description}

\noindent Table~\ref{tab:model-grids} lists the hyperparameter axes and candidate values for each detector. \footnote{\texttt{n\_runs} is the Cartesian size when unconditional axes are combined (\(\prod\) list lengths). One-class SVM mixes kernel-dependent parameters, so only valid library configurations are materialised rather than a naive full product. Section~\ref{sec:experiments} explains which parts of this space enter the primary benchmark and which enter the focused studies.}

\begin{table*}[!ht]
\centering
\small
\caption{Hyperparameter axes by model; \texttt{n\_runs} is the Cartesian axis count.}
\label{tab:model-grids}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{1.35cm} >{\raggedright\arraybackslash}p{2.45cm} >{\raggedright\arraybackslash}p{2.95cm} >{\raggedright\arraybackslash}X >{\centering\arraybackslash}p{1.05cm}}
\toprule
\textbf{Model} & \textbf{Parameter} & \textbf{Values} & \textbf{Motivation} & \textbf{\texttt{n\_runs}} \\
\midrule
\multirow{5}{*}{OCSVM}
& \texttt{kernel}
& \{rbf, poly, sigmoid, linear\}
& Compare boundary families and kernel scalability.
& \multirow{5}{*}{$^\dagger$} \\
& \texttt{nu}
& \{0.01, 0.05, 0.10, 0.20, 0.35, 0.50\}
& Upper bound training errors / fraction of outliers.
& \\
& \texttt{gamma}
& \{scale, auto, 0.001, 0.01, 0.1, 1.0, 10.0\}
& RBF / poly / sigmoid scaling; diagnoses kernel collapse regimes.
& \\
& \texttt{degree}
& \{2, 3, 4\}
& Polynomial depth (poly kernel).
& \\
& \texttt{coef0}
& \{0.0, 1.0\}
& Affine term for poly / sigmoid kernels.
& \\
\midrule
\multirow{6}{*}{IForest}
& \texttt{n\_estimators}
& \{50, 100, 200, 500\}
& Check ensemble convergence and variance reduction.
& \multirow{6}{*}{1008} \\
& \texttt{max\_samples}
& \{auto, 32, 64, 128, 256, 512\}
& Probe \(\psi\) / subsampling depth per Liu et~al.; \texttt{auto} clamps to \(\min(n,256)\).
& \\
& \texttt{contamination}
& \{0.01, 0.05, 0.10, 0.20, 0.35, 0.50\}
& Prior-dependent threshold shaping.
& \\
& \texttt{max\_features}
& \{0.5, 0.75, 1.0\}
& Feature bagging breadth for each tree split.
& \\
& \texttt{bootstrap}
& \{True, False\}
& Compare with vs.\ without replacement.
& \\
& \texttt{random\_state}
& \{42\}
& Locked reproducibility knob in exploratory sweeps.
& \\
\midrule
\multirow{5}{*}{LOF}
& \texttt{n\_neighbors}
& \{5, 10, 15, 20, 30, 50, 75, 100\}
& Locality scale controlling reachability densities.
& \multirow{5}{*}{480} \\
& \texttt{metric}
& \{euclidean, manhattan, minkowski, cosine\}
& Geometry stress for curse-of-dimensionality probes.
& \\
& \texttt{p}
& \{1, 2, 3\}
& Minkowski exponent when metric=\texttt{minkowski}.
& \\
& \texttt{contamination}
& \{0.01, 0.05, 0.10, 0.20, 0.35\}
& Prior-dependent thresholds on negative outlier factors.
& \\
& \texttt{novelty}
& \{False\}
& Transductive training scores (no withheld calibration split).
& \\
\midrule
\multirow{4}{*}{DBSCAN}
& \texttt{eps}
& \(\texttt{eps}_{knee}\times\{0.5,\dots,2.0\}\), six multipliers total
& Knee anchored on sorted \(k\text{-distance}\); multipliers perturb neighborhood radius.
& \multirow{4}{*}{72} \\
& \texttt{min\_samples}
& \{3, 5, 10, 15, 20, 30\}
& Core-point cardinality control.
& \\
& \texttt{metric}
& \{euclidean, manhattan\}
& Metric-dependent density connectivity tests.
& \\
& \texttt{algorithm}
& \{auto\}
& sklearn NN backend fallback (BallTree/KD/BF).
& \\
\midrule
ECOD
& \texttt{contamination}
& \{0.01, 0.05, 0.10, 0.20, 0.35, 0.50\}
& Tail-threshold sliders (scores unaffected).
& 6 \\
\midrule
\multirow{4}{*}{HBOS}
& \texttt{n\_bins}
& \{5, 10, 20, 30, 50, auto\}, \texttt{auto}\(\rightarrow\lfloor\sqrt{n}\rfloor\)
& Histogram fidelity vs.\ sparsity; PyOD heuristic for \texttt{auto}.
& \multirow{4}{*}{240} \\
& \texttt{alpha}
& \{0.0, 0.1, 0.2, 0.5\}
& Laplace smoothing of empty bins (\(\alpha\in[0,1)\) constraint).
& \\
& \texttt{tol}
& \{0.1, 0.5\}
& Numerical tolerance for tails when mapping to bins.
& \\
& \texttt{contamination}
& \{0.01, 0.05, 0.10, 0.20, 0.35\}
& Prior thresholds on aggregated log-scores.
& \\
\bottomrule
\end{tabularx}
\vspace{-0.2em}{\footnotesize $^\dagger$OCSVM: the RBF core alone has \(6~\nu \times 7~\gamma=42\) combinations; additional kernels extend the table. Focused one-class SVM analyses (Sobol sampling, Latin Hypercube exploration, and the ionosphere oracle sweep) are summarised in Section~\ref{sec:experiments}.}
\end{table*}

\section{Datasets}
\label{sec:datasets}

\subsection{Dataset Sources and Composition}

The benchmark corpus combines tabular anomaly detection datasets from ODDS \cite{rayana2016odds} with selected two-dimensional datasets from the Gagolewski clustering suite \cite{gagolewski2022clustering}. All data are standardized to a common schema with feature matrix \(X \in \mathbb{R}^{n \times d}\) and binary label vector \(y \in \{0, 1\}^n\), where \(1\) denotes an anomaly. The corpus contains 37 datasets and 1{,}086{,}545 observations, with \(n \in [120, 567{,}498]\), \(d \in [2, 400]\), and natural contamination rates from \(0.0\%\) to \(\approx 35.9\%\) (Appendix~A). Nine datasets have zero labeled outliers and are retained as a visual-only cohort.

The ODDS subset contains 26 multidimensional tabular datasets (via the ADBench mirror where needed \cite{rayana2016odds}). The 2D subset contains 11 Gagolewski datasets (v1.1.0), selected to stress geometric effects such as rings, interleaved curves, sparse neighborhoods, and ambiguous boundaries.

\subsection{Corpus Design Rationale}

The corpus is intentionally heterogeneous to expose distinct detector failure modes:

\begin{description}
    \item[High-dimensional stress] \texttt{arrhythmia} (d=274), \texttt{musk} (d=166), \texttt{speech} (d=400), and \texttt{mnist} (d=100) test kernel collapse and distance-metric degradation.
    \item[Extreme scale] \texttt{cover} (n=286{,}048), \texttt{http} (n=567{,}498), \texttt{smtp} (n=95{,}156), and \texttt{shuttle} (n=49{,}097) test algorithmic scalability and numerical stability.
    \item[Prior mismatch] \texttt{breastw} (35.0\%), \texttt{ionosphere} (35.9\%), \texttt{pima} (34.9\%), and \texttt{satellite} (31.6\%) test methods sensitive to contamination assumptions when the prior is far from typical (1--5\%).
    \item[Geometric complexity] Gagolewski 2D datasets expose solver sensitivity to local versus global anomaly structure, symmetry, and boundary ambiguity.
\end{description}

\subsection{Data Acquisition and Preprocessing}

Dataset acquisition and preprocessing follow a uniform protocol:

\begin{description}
    \item[Download and validation] Raw artifacts are downloaded from verified public sources (ADBench mirrors for ODDS; official GitHub release for Gagolewski v1.1.0). Each dataset is checked against expected row count, feature count, and contamination.
    \item[Canonicalization] All datasets are converted to a common tabular format (CSV or NumPy) with explicit binary labels. Missingness is documented; \texttt{arrhythmia} uses a dedicated iterative treatment (below).
    \item[Default preprocessing] Unless a dedicated pipeline is defined, numeric features are robust-scaled (median centering and IQR normalization). This reduces outlier leverage in scale estimation while preserving low-dimensional interpretability.
    \item[Arrhythmia-specific preprocessing] In the data actually used for this study, the ODDS-style \texttt{arrhythmia} table has no CSV-level pandas \texttt{NaN}s (Appendix~A missing fraction \(=0\)). Therefore, imputation is inactive for this dataset in all reported runs. The pipeline then applies one-hot encoding and keeps both an engineered high-dimensional view and a PCA-95 view. The \(95\%\) retained-variance target follows standard PCA retention practice \cite{jolliffe2002pca}. In the main benchmark, LOF and DBSCAN use PCA-95 only; IForest, OCSVM, ECOD, and HBOS also use the engineered view.
\end{description}

\subsection{Algorithm-Specific Subsampling}

Computational cost varies substantially across algorithms. To keep runs tractable, I apply:

\begin{description}
    \item[No subsampling] IForest, ECOD, and HBOS run on full datasets due to favorable scaling (\(O(n \log n)\) or \(O(nd \log n)\)).
    \item[Stratified subsampling] OCSVM, LOF, and DBSCAN (quadratic or worse in common settings) are capped at \(n=20{,}000\) when the full dataset is larger. Stratified sampling preserves contamination while keeping runtime and memory feasible on very large datasets. This affects \texttt{cover}, \texttt{http}, \texttt{smtp}, and \texttt{shuttle}.
\end{description}

\noindent Anomaly labels are used for evaluation only and never for fitting or hyperparameter selection. For quantitative ranking and aggregate metric summaries, I evaluate only the 28 datasets with at least one labeled outlier; the 9 zero-outlier datasets are excluded from thresholded and positive-class metrics and used only for qualitative geometry visualizations.

\begin{description}
    \item[Preprocessing comparability caveat.] Local methods (LOF, DBSCAN) are evaluated on PCA-95 representations, while IForest, OCSVM, ECOD, and HBOS also use engineered non-PCA views in the benchmark pool. Cross-family comparisons should therefore be interpreted as benchmark-level evidence under mixed preprocessing rather than strict feature-parity comparisons.
\end{description}

\noindent Detailed corpus-level and per-dataset statistics are provided in Appendix~A.

\section{Experiments}
\label{sec:experiments}

Table~\ref{tab:model-grids} defines the full candidate hyperparameter space. The runs below specify the executed protocol.

\subsection{Two layers: benchmark and deep dives}

The study has two layers. The \textbf{primary benchmark} fits each detector once per dataset with one unsupervised default configuration (no labels for model selection). Defaults follow common library practice: IForest with a medium ensemble and fixed subsample cap, LOF with moderate neighborhood size, DBSCAN with knee-estimated radius \cite{satopaa2011kneedle}, RBF OCSVM with moderate \(\nu\), and library defaults for ECOD/HBOS. Stratified subsampling for slower detectors at large \(n\) is defined in Section~\ref{sec:datasets}.

The \textbf{focused studies} expand selected hyperparameters in regimes expected to expose robustness effects (geometry for local methods, neighborhoods for clustering, contamination priors for tail models). Settings are repeated with five seeds \(\{0,1,2,3,42\}\) to estimate variability. I do not exhaustively evaluate every cell of Table~\ref{tab:model-grids} on every dataset.

\subsection{Focused hyperparameter sweeps}

\begin{description}
    \item[Isolation Forest] on \texttt{breastw}: forest sizes \(\{50,100,200,500\}\) trees, subsample sizes \(128\) and \(256\), contamination priors \(0.05\) and \(0.10\) for thresholding, bagging bootstrap off, five seeds each.

    \item[Local outlier factor] on \texttt{satellite}: eight neighbour counts spaced from \(5\) to \(100\), Euclidean distance, five seeds; the same neighbour grid is repeated under Manhattan, cosine, and Minkowski \((p{=}3)\) metrics with identical seeds.

    \item[DBSCAN] on \texttt{satellite}: six scalings of a knee-derived radius, six \texttt{min\_samples} values from sparse to denser cores, Euclidean or Manhattan geometry, five seeds.

    \item[ECOD] on \texttt{pima}: six contamination assumptions for translating scores into decisions.

    \item[Histogram-based detection] on \texttt{satellite}: five discrete bin layouts (the automatic bin-count heuristic is switched off here), three Laplace-smoothing levels for empty bins, five seeds.
\end{description}

\subsection{Sensitivity to hyperparameters (variance-based)}

I estimate first-order Sobol indices for PR-AUC using a Saltelli-style quasi-random scheme with \(N_{\text{saltelli}}=28\) base draws per dataset--model pair. Isolation Forest is analysed on \texttt{ionosphere}, \texttt{breastw}, and \texttt{smtp}; RBF one-class SVM on \texttt{breastw}, \texttt{ionosphere}, and \texttt{pima}. In parallel, eighty Latin Hypercube draws explore \(\nu\) and \(\gamma\) pairs from the categorical RBF core on the same three SVM probe datasets---a complementary view that does not require treating \(\gamma\) as a continuous surrogate. Given this sample budget, Sobol summaries are interpreted as indicative and aligned with the evaluation-variance caution emphasized by Campos et al. \cite{campos2016evaluation}.

\noindent\textbf{Scope distinction.} True Sobol indices (with bootstrap confidence intervals) are reported only for IForest and OCSVM (Fig.~\ref{fig:results-sobol-ci}). For LOF, DBSCAN, ECOD, and HBOS, Fig.~\ref{fig:results-param-importance} reports permutation importance from random-forest surrogates fitted to run-level sweeps; these are surrogate-based sensitivity proxies, not Sobol decompositions.

\subsection{Oracle sweep and bootstrap stability}

As a retrospective upper bound on RBF one-class SVM tuning, every \(\nu{\times}\gamma\) pair from the categorical RBF grid is evaluated on \texttt{ionosphere} with five seeds (210 pooled fits total).

Isolation Forest stability on \texttt{thyroid}: forty-eight hyperparameter tuples are picked deterministically from a dense Cartesian surrogate; each tuple receives ten stratified bootstrap replicates that retain roughly \(90\%\) of points. I favour tuples with high mean PR-AUC among replicates whose bootstrap standard deviation \(\sigma_{\mathrm{boot}}\) stays at or below \(0.03\); if none qualify, I keep the strongest mean-score candidate regardless.

\section{Evaluation Methodology}
\label{sec:evaluation}

The evaluation protocol separates pointwise performance from robustness analysis. Metrics summarize thresholded classification and ranking quality under a fixed contamination rule; sensitivity analyses quantify variation under hyperparameter changes and resampling.

\subsection{Metrics}

All thresholded metrics use the natural contamination cut, so each method predicts the expected outlier fraction for that dataset. This keeps thresholding fixed across methods and shifts comparison to score quality rather than post-hoc calibration.

\begin{description}
    \item[Oracle-threshold caveat.] The natural contamination cut is label-informed and should be interpreted as an oracle-assisted analysis setting rather than a deploy-time unsupervised operating point. I therefore foreground threshold-independent metrics (ROC-AUC, PR-AUC) in cross-method comparisons and use thresholded metrics as conditional diagnostics under known class prevalence.

        Oracle-threshold metrics are not used as the primary basis for cross-method ranking claims; they are optimistic diagnostics under known prevalence, not deploy-time operating-point evidence.

    \item[Contamination-parameter semantics.] In IForest, LOF, ECOD, and HBOS, the \texttt{contamination} parameter changes only the decision cutoff on already-computed anomaly scores; it does not alter the score-generation mechanism itself. In contrast, for OCSVM, \(\nu\) shapes the learned support boundary (model fit) and also influences the implied outlier fraction. I therefore interpret OCSVM \(\nu\)-sensitivity as model-behavior sensitivity, while contamination sweeps for the other families are interpreted primarily as threshold-calibration sensitivity.
\end{description}

\noindent Under the natural contamination cut, the number of predicted positives equals the number of true positives by construction; therefore precision and recall are numerically identical, and \(F1\) is identical as well. To avoid redundant reporting, the main tables retain recall only among \{precision, recall, F1\}.

As a sensitivity check, I also evaluate thresholded metrics using elbow-estimated contamination rates (same score vectors, dataset-specific estimated prevalence). Relative to oracle thresholds on the matched 28-dataset subset (Table~\ref{tab:metrics-estimated-vs-oracle}), recall decreases across all methods, confirming that oracle-threshold metrics are optimistic for deployment calibration. In downstream interpretation, this estimated-threshold view is treated as the primary thresholded reference, while oracle-threshold tables are retained as supplementary diagnostics.

\begin{description}
    \item[Accuracy] is the fraction of correct labels, \(\frac{TP+TN}{TP+TN+FP+FN}\). I retain it as a baseline summary, but interpret it cautiously because it can be dominated by the majority class under extreme imbalance.
    \item[Precision] is \(\frac{TP}{TP+FP}\): among predicted outliers, the proportion that are true outliers. High precision means few false alarms.
    \item[Recall] is \(\frac{TP}{TP+FN}\): the fraction of true outliers recovered. This is important when missing anomalies is expensive.
    \item[F1] is the harmonic mean of precision and recall, \(F1=\frac{2PR}{P+R}\). It balances missed anomalies and false alarms in one thresholded score.
    \item[ROC-AUC] is the area under the ROC curve (TPR vs FPR over thresholds), where \(\mathrm{TPR}=\frac{TP}{TP+FN}\) and \(\mathrm{FPR}=\frac{FP}{FP+TN}\). It evaluates ranking quality independently of class prevalence, so it is useful for cross-dataset comparison (except for DBSCAN, where binary outputs induce a degenerate single-point area).
    \item[PR-AUC] is the area under the precision-recall curve, where precision \(=\frac{TP}{TP+FP}\) and recall \(=\frac{TP}{TP+FN}\) are traced over score thresholds. Intuitively, it measures how well a method retrieves true anomalies while limiting false alarms. Because anomalies are rare in this benchmark, PR-AUC is often more informative than ROC-AUC for practical ranking (with the same DBSCAN degeneracy caveat).
    \item[MCC] (Matthews correlation coefficient) is a single-score summary of the confusion matrix:
    \[
    \resizebox{0.96\columnwidth}{!}{$
    \mathrm{MCC}=\frac{TP\cdot TN-FP\cdot FN}
    {\sqrt{(TP+FP)(TP+FN)(TN+FP)(TN+FN)}}
    $}
    \]
    MCC ranges from \(-1\) (total disagreement) through \(0\) (chance-level) to \(1\) (perfect agreement), so it remains interpretable under strong class imbalance where accuracy can be misleading.
\end{description}

\subsection{Sensitivity and robustness reporting}

Robustness summaries accompany the main metrics and quantify (i) PR-AUC movement under controlled hyperparameter changes and (ii) score stability under bootstrap resampling. Schedules are listed in Section~\ref{sec:experiments}. For reproducibility, primary defaults are IForest (\(n_{\text{estimators}}=200\), \(\texttt{max\_samples}=256\), bootstrap off), LOF (\(k=20\)), DBSCAN (knee \(\epsilon\), \(\texttt{min\_samples}=5\)), RBF OCSVM (\(\nu=0.1\)), and library defaults for ECOD/HBOS. Labels are never used for hyperparameter selection.

\section{Results}
\label{sec:results}

This section reports the main empirical patterns without overinterpreting aggregate plots. Unless noted otherwise, summaries use per-dataset medians across benchmark runs, with seed repeats and tested variants pooled.

\subsection{Within-family performance summary}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/topology_pr_auc_distribution.png}
\caption{PR-AUC distribution across pooled benchmark runs (including seed repeats and tested variants) for the engineered/non-PCA-capable family only (OCSVM, IForest, ECOD, HBOS) on the quantitative cohort. LOF and DBSCAN are excluded because they were evaluated on PCA-95-only representations and are summarized separately in Table~\ref{tab:metrics-summary-pca-local}.}
\label{fig:results-prauc-dist}
\end{figure}

The pooled PR-AUC distribution in Fig.~\ref{fig:results-prauc-dist} is shown only for the engineered/non-PCA-capable family to preserve feature parity inside the visual. LOF and DBSCAN are compared only against each other in the PCA-95 local-method family (Table~\ref{tab:metrics-summary-pca-local}), while OCSVM, IForest, ECOD, and HBOS are compared within the engineered/non-PCA-capable family (Table~\ref{tab:metrics-summary-engineered}).

Within the PCA-95 local family, DBSCAN exceeds LOF on thresholded metrics (recall 0.321 vs 0.212; MCC 0.253 vs 0.133). DBSCAN AUC values are not compared because binary outputs do not define a full ranking curve. Within the engineered-capable family, IForest has the strongest aggregate profile (PR-AUC \(0.457\), MCC \(0.386\)), followed by ECOD and HBOS, with OCSVM competitive but lower on average PR-AUC and MCC.

\begin{table*}[!ht]
\centering
\caption{Primary thresholded benchmark view (deployment-oriented, mixed-preprocessing/non-feature-parity): absolute mean metrics at elbow-estimated contamination thresholds on the matched 28-dataset subset. ROC-AUC and PR-AUC are threshold-independent and therefore unchanged by threshold calibration; they are repeated here for completeness.}
\label{tab:metrics-estimated-absolute}
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{l
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]}
\toprule
\textbf{Method} & \textbf{Accuracy} & \textbf{Precision} & \textbf{Recall} & \textbf{MCC} & \textbf{F1} & \textbf{ROC-AUC} & \textbf{PR-AUC} \\
\midrule
OCSVM   & \num{0.874} & \num{0.396} & \num{0.207} & \num{0.170} & \num{0.167} & \num{0.734} & \num{0.354} \\
IForest & \num{0.882} & \num{0.472} & \num{0.270} & \num{0.249} & \num{0.239} & \num{0.818} & \num{0.457} \\
LOF     & \num{0.875} & \num{0.236} & \num{0.208} & \num{0.118} & \num{0.129} & \num{0.668} & \num{0.228} \\
DBSCAN  & \num{0.873} & \num{0.405} & \num{0.205} & \num{0.167} & \num{0.163} & \num{0.677} & \num{0.317} \\
ECOD    & \num{0.879} & \num{0.392} & \num{0.249} & \num{0.203} & \num{0.200} & \num{0.776} & \num{0.396} \\
HBOS    & \num{0.879} & \num{0.336} & \num{0.235} & \num{0.189} & \num{0.195} & \num{0.762} & \num{0.367} \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[!ht]
\centering
\caption{Estimated-threshold sensitivity complement (mixed-preprocessing, non-feature-parity): mean metric delta (estimated contamination threshold minus oracle natural-contamination threshold) on the matched 28-dataset subset.}
\label{tab:metrics-estimated-vs-oracle}
\small
\setlength{\tabcolsep}{7pt}
\begin{tabular}{l
                S[table-format=-1.3]
                S[table-format=-1.3]
                S[table-format=-1.3]
                S[table-format=-1.3]
                S[table-format=-1.3]
                S[table-format=-1.3]}
\toprule
\textbf{Metric} & \textbf{OCSVM} & \textbf{IForest} & \textbf{LOF} & \textbf{DBSCAN} & \textbf{ECOD} & \textbf{HBOS} \\
\midrule
$\Delta$ Accuracy & \num{-0.016} & \num{-0.034} & \num{-0.001} & \num{-0.020} & \num{-0.025} & \num{-0.024} \\
$\Delta$ Recall   & \num{-0.132} & \num{-0.169} & \num{-0.005} & \num{-0.115} & \num{-0.141} & \num{-0.125} \\
$\Delta$ MCC      & \num{-0.098} & \num{-0.137} & \num{-0.015} & \num{-0.086} & \num{-0.126} & \num{-0.110} \\
\bottomrule
\end{tabular}
\end{table*}

\begin{description}
    \item[Supplementary Oracle-Threshold Diagnostics] The following oracle-threshold tables are retained for diagnostic calibration context only and are not used as primary deployment-facing performance evidence.

    \item[Coverage note.] The quantitative cohort has 28 datasets with labeled outliers, and all 28 are included in Tables~\ref{tab:metrics-summary-pca-local}, \ref{tab:metrics-summary-engineered}, and \ref{tab:metrics-estimated-vs-oracle}. A prior export bug that skipped slash-delimited dataset identifiers (including \texttt{sipu/flame} and \texttt{wut/x2}) has been fixed in the phase-6 artifact pipeline.
\end{description}

\begin{table}[!ht]
\centering
\caption{Supplementary oracle-threshold diagnostic (PCA-95-only local methods): mean $\pm$ std metrics at natural-contamination thresholds on datasets with labeled outliers and available score vectors (\(n=28\)). Precision and F1 are omitted because they are identical to recall by construction under this thresholding rule. DBSCAN ROC-AUC/PR-AUC are listed as N/A because DBSCAN emits binary outputs and does not provide a comparable ranking curve.}
\label{tab:metrics-summary-pca-local}
\scriptsize
\begin{tabularx}{\columnwidth}{l *{5}{>{\centering\arraybackslash}X}}
\toprule
\textbf{Method} & \textbf{Accuracy} & \textbf{Recall} & \textbf{ROC-AUC} & \textbf{PR-AUC} & \textbf{MCC} \\
\midrule
LOF & 0.876 $\pm$ 0.142 & 0.212 $\pm$ 0.204 & 0.668 $\pm$ 0.183 & 0.228 $\pm$ 0.218 & 0.133 $\pm$ 0.217 \\
DBSCAN & 0.894 $\pm$ 0.123 & 0.321 $\pm$ 0.276 & N/A (binary output) & N/A (binary output) & 0.253 $\pm$ 0.292 \\
\bottomrule
\end{tabularx}
\end{table}

\begin{table}[!ht]
\centering
\caption{Supplementary oracle-threshold diagnostic (engineered/non-PCA-capable methods): mean $\pm$ std metrics at natural-contamination thresholds on datasets with labeled outliers and available score vectors (\(n=28\)).}
\label{tab:metrics-summary-engineered}
\scriptsize
\begin{tabularx}{\columnwidth}{l *{4}{>{\centering\arraybackslash}X}}
\toprule
\textbf{Metric} & \textbf{OCSVM} & \textbf{IForest} & \textbf{ECOD} & \textbf{HBOS} \\
\midrule
Accuracy & 0.890 $\pm$ 0.121 & 0.915 $\pm$ 0.089 & 0.904 $\pm$ 0.106 & 0.903 $\pm$ 0.112 \\
Recall & 0.339 $\pm$ 0.227 & 0.439 $\pm$ 0.336 & 0.390 $\pm$ 0.274 & 0.360 $\pm$ 0.310 \\
ROC-AUC & 0.734 $\pm$ 0.192 & 0.818 $\pm$ 0.178 & 0.776 $\pm$ 0.207 & 0.762 $\pm$ 0.209 \\
PR-AUC & 0.354 $\pm$ 0.227 & 0.457 $\pm$ 0.360 & 0.396 $\pm$ 0.291 & 0.367 $\pm$ 0.328 \\
MCC & 0.268 $\pm$ 0.235 & 0.386 $\pm$ 0.347 & 0.329 $\pm$ 0.287 & 0.299 $\pm$ 0.324 \\
\bottomrule
\end{tabularx}
\end{table}

\noindent MCC trends are directionally consistent with PR-AUC and ROC-AUC but add imbalance-aware perspective. Within the engineered-capable family, IForest has the strongest mean MCC (\(0.386\)), followed by ECOD (\(0.329\)) and HBOS (\(0.299\)); within the PCA-95 local family, DBSCAN exceeds LOF (0.253 vs 0.133). These values are interpreted within family only.

For ranking interpretation, Table~\ref{tab:metrics-estimated-absolute} is the primary thresholded deployment-oriented view, while Table~\ref{tab:metrics-estimated-vs-oracle} provides sensitivity deltas. Tables~\ref{tab:metrics-summary-pca-local} and \ref{tab:metrics-summary-engineered} are supplementary oracle diagnostics only. All benchmark-level cross-family statements remain caveated as mixed-preprocessing, non-feature-parity evidence.

\begin{table*}[!ht]
\centering
\caption{Supplementary preprocessing ablation (IForest, representative subset): engineered default view (\texttt{track-a-defaults}) versus high-variance PCA view (\texttt{pca-0.99}) on datasets where both were run with matched five-seed repeats. This table quantifies preprocessing sensitivity directly; signs and magnitudes show that representation choice can materially alter outcomes, so cross-family comparisons under mixed preprocessing are treated as non-parity evidence.}
\label{tab:iforest-preprocess-ablation}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{l
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]
                S[table-format=1.3]}
\toprule
\textbf{Dataset} & {\textbf{PR-AUC (Eng.)}} & {\textbf{PR-AUC (PCA)}} & {\(\Delta\) \textbf{PR-AUC}} & {\textbf{ROC-AUC (Eng.)}} & {\textbf{ROC-AUC (PCA)}} & {\(\Delta\) \textbf{ROC-AUC}} \\
\midrule
arrhythmia & \num{0.421} & \num{0.384} & \num{-0.037} & \num{0.789} & \num{0.780} & \num{-0.009} \\
mnist      & \num{0.277} & \num{0.329} & \num{0.052}  & \num{0.808} & \num{0.817} & \num{0.010} \\
musk       & \num{0.999} & \num{0.228} & \num{-0.771} & \num{1.000} & \num{0.904} & \num{-0.096} \\
speech     & \num{0.023} & \num{0.019} & \num{-0.004} & \num{0.474} & \num{0.503} & \num{0.029} \\
\midrule
mean (subset) & \num{0.430} & \num{0.240} & \num{-0.190} & \num{0.768} & \num{0.751} & \num{-0.017} \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Stability and robustness}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/topology_seed_fragility_heatmap.png}
\caption{Seed fragility heatmap; each value is \(\mathrm{std}(\mathrm{PR\text{-}AUC})\) across seeds for one dataset--algorithm group.}
\label{fig:results-seed-fragility}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/topology_seed_stability_bars.png}
\caption{Algorithm-level seed stability summary; median seed std and median seed range are aggregated over dataset-level seed repeats.}
\label{fig:results-seed-stability-bars}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q1_variance_decomposition.png}
\caption{Variance decomposition separating between-dataset variability from within-dataset seed variability.}
\label{fig:results-variance-decomp}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q2_iforest_vs_lof_dimensionality.png}
\caption{Dimensional robustness contrast; points are per-dataset median PR-AUC values (all runs pooled), with trend lines for IForest and LOF.}
\label{fig:results-q2-dim}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q3_ecod_lof_gap_vs_correlation.png}
\caption{ECOD--LOF gap versus correlation; each point is one dataset, and the vertical axis is \(|\mathrm{PR\text{-}AUC}_{ECOD}-\mathrm{PR\text{-}AUC}_{LOF}|\) from per-dataset medians.}
\label{fig:results-q3-corr}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q4_pr_auc_by_outlier_regime.png}
\caption{ODDS tabular stratification by outlier regime (Campos-style local/cluster vs global/point grouping): per-dataset median PR-AUC distributions by algorithm and regime.}
\label{fig:results-q4-regime}
\end{figure}

Two patterns are stable across robustness views (Figs.~\ref{fig:results-seed-fragility}--\ref{fig:results-q4-regime}). First, seed noise is small relative to dataset heterogeneity: for most methods, median seed-level \(\mathrm{std}(\mathrm{PR\text{-}AUC})\) is effectively \(0\), while between-dataset variation remains large. Second, structural covariates still matter. In this corpus, PR-AUC slope versus \(\log(d)\) is positive for all methods (associative trend only), and the absolute ECOD--LOF performance gap is positively associated with mean feature correlation (Pearson \(r\approx0.57\), 95\% CI \([0.30, 0.75]\); also associative). The positive dimensional slope is likely corpus-composition dependent rather than a universal claim, because several high-dimensional datasets also show low distance-concentration ratios that should penalize local distance-based methods. These are associative patterns, not causal claims.

To directly quantify local-vs-global failure behavior on tabular datasets, I assign ODDS datasets to two outlier regimes (global/point-dominant vs local/cluster-dominant) using Campos-style taxonomy criteria and stratify PR-AUC by that axis (Fig.~\ref{fig:results-q4-regime}; Appendix~B mapping). This yields the expected contrast: LOF improves strongly on local/cluster-dominant datasets (mean PR-AUC \(0.272\) vs \(0.073\) on global/point), while IForest remains comparatively strong in both regimes (global/point \(0.494\), local/cluster \(0.417\)). DBSCAN and ECOD also show regime dependence, but with smaller separation than LOF.

\subsection{Method redundancy and contamination estimation}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q4_kendall_tau_heatmap.png}
\caption{Kendall rank-correlation matrix computed from aligned per-point rank scores across methods.}
\label{fig:results-kendall}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q5_elbow_parity.png}
\caption{Elbow contamination parity; elbow estimates are compared with known contamination rates on the same datasets.}
\label{fig:results-elbow-parity}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q4_pairwise_tau_distribution.png}
\caption{Pairwise Kendall distribution; each box summarizes dataset-wise Kendall \(\tau\) values for one algorithm pair.}
\label{fig:results-kendall-distribution}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q5_elbow_residual_diagnostics.png}
\caption{Elbow residual diagnostics; residual \(=\) estimated minus true contamination, plotted against contamination and dimensionality.}
\label{fig:results-elbow-residuals}
\end{figure}

The redundancy analysis (Figs.~\ref{fig:results-kendall}--\ref{fig:results-kendall-distribution}) shows overlap but not interchangeability. The strongest pairwise agreement is IForest--ECOD (\(\tau\approx0.69\)); under a no-tie interpretation this corresponds to roughly \(84.5\%\) concordant rank pairs \((\tau+1)/2\). This indicates high agreement on this benchmark corpus, not universal method redundancy, because IForest (isolation-based, interaction-aware) and ECOD (marginal tail-based) encode different inductive biases and can diverge on datasets where interaction structure dominates. A secondary alignment is OCSVM--IForest (\(\tau\approx0.43\)), showing that boundary-based OCSVM is closer to global tree/rank detectors than to purely local neighborhood methods. LOF and DBSCAN remain comparatively weakly aligned with global methods (e.g., DBSCAN--IForest \(\tau\approx0.31\), DBSCAN--LOF \(\tau\approx0.22\)), which is the empirical basis for combining local and global detectors in ensembles. Consistent with outlier-ensemble guidance, I treat rank correlation as one diversity signal among several rather than a stand-alone redundancy criterion \cite{aggarwal2015outlierensembles}.

For contamination estimation (Figs.~\ref{fig:results-elbow-parity}--\ref{fig:results-elbow-residuals}), elbow parity is mixed. Median absolute error is \(0.031\), but the mean absolute error rises to \(0.081\) due to a heavy tail, and residuals are negatively biased on average (\(-0.049\), underestimation). Elbow estimates are therefore useful as initialization, but not as a stand-alone calibration rule on high-contamination datasets.

\subsection{2D geometry evidence}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/2d_compare_score_maps__wut__x2.png}
\caption{All-model score maps on one 2D dataset with shared score scale, selected hyperparameters per model, and \(x\)-markers for predicted anomalies.}
\label{fig:results-2d-allmodels}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/2d__wut__x2__IForest.png}
\caption{Single-model 2D panel set (IForest on \texttt{wut/x2}) showing the raw map, score map, binary predictions, and PR curve.}
\label{fig:results-2d-single}
\end{figure}

The 2D diagnostics (Figs.~\ref{fig:results-2d-allmodels}--\ref{fig:results-2d-single}) make detector behavior visually interpretable: local-density methods react to neighborhood structure, while marginal/global methods emphasize pointwise rarity. I treat these plots as qualitative mechanism checks that complement, rather than replace, the corpus-level metrics.

\subsection{Hyperparameter importance}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/deepdive_param_importance_per_algorithm.png}
\caption{Per-algorithm parameter importance from permutation analysis on random-forest surrogates fitted to run-level hyperparameter configurations. Surrogate fit (\(R^2\)) by method: OCSVM \(0.514\), IForest \(0.586\), LOF \(0.213\), DBSCAN \(0.348\), ECOD \(0.053\), HBOS \(0.273\). Importance bars for methods with \(R^2<0.2\) (ECOD) are treated as unreliable.}
\label{fig:results-param-importance}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/deepdive_iforest_samples_estimators_heatmap.png}
\caption{IForest hyperparameter response surface showing median PR-AUC over \texttt{max\_samples} and \texttt{n\_estimators} across all IForest runs. Colormap is normalized to the observed PR-AUC range (viridis) for visual legibility.}
\label{fig:results-iforest-surface}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/deepdive_sobol_s1_with_ci.png}
\caption{Sobol first-order indices for IForest and OCSVM with 95\% bootstrap confidence intervals (resampling across probe datasets). Intervals are wide under the current Saltelli budget, so separation between leading factors should be treated as low-confidence and directional only (indicative, not definitive).}
\label{fig:results-sobol-ci}
\end{figure}

\begin{figure*}[!ht]
\centering
\includegraphics[width=0.48\textwidth]{../outputs/figures/deepdive_lof_neighbors_metric_heatmap.png}
\hfill
\includegraphics[width=0.48\textwidth]{../outputs/figures/deepdive_dbscan_eps_min_samples_heatmap.png}

\vspace{0.4em}
\includegraphics[width=0.48\textwidth]{../outputs/figures/deepdive_ecod_contamination_line.png}
\hfill
\includegraphics[width=0.48\textwidth]{../outputs/figures/deepdive_hbos_bins_alpha_heatmap.png}
\caption{Per-method sensitivity surfaces for methods not covered by Sobol plots. Top-left: LOF neighbor-count and distance-metric sweep. Top-right: DBSCAN epsilon-multiplier and min-samples sweep. Bottom-left: ECOD contamination-to-label sweep on pima. Bottom-right: HBOS bin-count and smoothing sweep.}
\label{fig:results-sensitivity-missing-methods}
\end{figure*}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q2_slope_by_algorithm.png}
\caption{Dimensional sensitivity summary: slope of median PR-AUC versus \(\log(n_{\text{features}})\) for each algorithm.}
\label{fig:results-q2-slope}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\linewidth]{../outputs/figures/q2_pr_auc_vs_log_n_rows.png}
\caption{Robustness versus dataset size: per-dataset median PR-AUC versus \(\log(n_{\text{rows}})\), colored by algorithm.}
\label{fig:results-q2-size}
\end{figure}

Figure~\ref{fig:results-param-importance} identifies dominant tuning axes via surrogate-permutation importance (not Sobol), and Fig.~\ref{fig:results-iforest-surface} shows this explicitly for IForest. Fig.~\ref{fig:results-sobol-ci} adds uncertainty bars for the true Sobol analysis (IForest/OCSVM only), while Fig.~\ref{fig:results-sensitivity-missing-methods} provides explicit response plots for LOF, DBSCAN, ECOD, and HBOS along their key tuning axes. For the surrogate-importance bars, no Sobol-style interval is claimed; interpretation is conditioned on surrogate fidelity (\(R^2\): OCSVM \(0.514\), IForest \(0.586\), LOF \(0.213\), DBSCAN \(0.348\), ECOD \(0.053\), HBOS \(0.273\)). Under the \(R^2<0.2\) rule, ECOD surrogate-importance bars are treated as unreliable and are not used for strong parameter-priority conclusions. In practice, this supports a staged strategy: tune high-impact parameters first, then refine secondary controls only when a dataset family shows persistent residual error. Because Sobol runs still use a limited base sample budget, those Sobol magnitudes remain directional rather than definitive despite interval reporting.

\subsection{Verification task summary}

For \texttt{test\_data.csv}, I use a rank-based ensemble over OCSVM, IForest, LOF, DBSCAN, ECOD, and HBOS. Ensemble use is motivated by the complementarity analysis in Section~\ref{sec:results}: global-method overlap is high, but local/global disagreement remains informative. Scores are converted to aligned rank scales and combined through weighted Borda aggregation, following modern outlier-ensemble guidance on rank-based fusion of heterogeneous detector outputs without assuming score-scale comparability \cite{aggarwal2015outlierensembles}. (The Borda rule itself originates from de Borda's historical formulation \cite{borda1781}.)

Preprocessing follows the benchmark policy: numeric features are robust-scaled, with compatibility transforms applied where needed. On \texttt{test\_data.csv}, no preprocessing anomalies were observed (numeric missing fraction \(=0\)); the prepared matrix has \(n=3443\) rows and \(d=22\) features after filtering. Hyperparameters follow the validated defaults from the primary benchmark (IForest: \(n_{\text{estimators}}=200\), \(\texttt{max\_samples}=256\), bootstrap off; LOF: \(k=20\); DBSCAN: knee-based \(\epsilon\) \cite{satopaa2011kneedle}, \(\texttt{min\_samples}=5\); OCSVM: RBF with \(\nu=0.1\); ECOD/HBOS: library defaults). These defaults were selected because they remained stable across the focused sweeps while preserving transferability across heterogeneous datasets.

Because labels are unavailable for \texttt{test\_data.csv}, I report both contamination signals used in the blind protocol: elbow on ensemble ranks gives \(\hat{c}_{elbow}=0.0224\), while the nearest-ODDS structural prior gives \(\hat{c}_{odds}=0.0533\) (profile distance \(0.017<2.0\)); the final threshold uses the ODDS-prior path under this distance criterion. The export step enforces assignment constraints: one \texttt{class} column, binary \(\{0,1\}\) values, and exact row-count parity with input. The resulting file is delivered as \texttt{test\_labels.csv}.

The next section synthesizes these empirical findings into practical guidance and method-level trade-offs.

\section{Discussion}
\label{sec:discussion}

The benchmark shows no universally best detector. Instead, performance depends on data topology, contamination regime, and geometric structure. Within-family comparisons indicate that IForest is strongest in the engineered/non-PCA-capable group, while DBSCAN remains competitive with LOF in the PCA-95 local group, indicating that local density-connectivity cues are decisive in specific cases.

Method weaknesses are also regime-dependent. LOF and DBSCAN are less transferable across heterogeneous datasets without careful neighborhood calibration, and their failure risk is associated with high-dimensional regimes where distance-concentration ratios compress toward \(1\) or below (Appendix~A CR column; Fig.~\ref{fig:results-q2-dim}). OCSVM is sensitive to boundary-shape and kernel-scale choices. ECOD and HBOS provide efficient and often competitive baselines, but marginal assumptions can miss interaction-driven anomalies. These trade-offs are consistent with the redundancy analysis: some method pairs overlap substantially, but low Kendall alignment for local methods confirms that they still add complementary signal.

From a practical standpoint, detector-specific operating rules are more useful than generic advice. Table~\ref{tab:practical-guidelines} summarizes actionable defaults and first-tuning moves per method under limited domain knowledge.

\begin{table}[!ht]
\centering
\caption{Practical detector-specific parameter guidelines for unsupervised deployment.}
\label{tab:practical-guidelines}
\small
\begin{tabularx}{\columnwidth}{p{1cm} p{1.5cm} X}
\toprule
\textbf{Method} & \textbf{Start point} & \textbf{First adjustment without labels} \\
\midrule
OCSVM & RBF, \(\nu=0.05\) to \(0.10\), \(\gamma=\) ``scale'' & If too many alerts, lower \(\nu\) before changing \(\gamma\); if boundary is too rigid (missed cluster-edge anomalies), increase \(\gamma\) one step. Treat \(\nu\) as model-shape control, not only thresholding. \\
IForest & \(n_{\text{estimators}}=200\), \(\texttt{max\_samples}=256\) & Increase trees to \(500\) only when rankings are unstable across seeds; tune \(\texttt{max\_samples}\) (128/256/512) before contamination cut. Contamination mainly sets decision threshold. \\
LOF & \(k=20\), metric=Euclidean on PCA-95 & If outputs are noisy, increase \(k\) (e.g., 20\(\rightarrow\)35); if local anomalies are missed, decrease \(k\) (e.g., 20\(\rightarrow\)10). Avoid raw high-\(d\) spaces when CR is low. \\
DBSCAN & \(\epsilon=\epsilon_{knee}\), \(\texttt{min\_samples}=5\) on PCA-95 & If almost everything is noise, raise \(\epsilon\) multiplier (\(1.25\times\), \(1.5\times\)); if almost nothing is noise, lower \(\epsilon\) (\(0.8\times\), \(0.6\times\)). Adjust \(\texttt{min\_samples}\) second (3--10). \\
ECOD & library default scoring, contamination from prior/elbow only for labels & Keep scores fixed and calibrate operating point via contamination prior bands (e.g., 1--5\%, 5--10\%). Prefer as stable global baseline when compute budget is tight. \\
HBOS & moderate bin count (auto or 15--25), light smoothing & If unstable across runs/datasets, reduce bins; if overly coarse, increase bins gradually. Use as fast coarse filter, then validate with one non-histogram method. \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Practical parameter-selection guidance}

Table~\ref{tab:practical-guidelines} is intended as a decision aid, but the practical logic is detector-specific and merits explicit narrative interpretation. For OCSVM, \(\nu\) should be treated as a structural control on boundary tightness rather than a pure post-hoc threshold knob: when false alarms dominate, reducing \(\nu\) is typically the first stabilizing move before changing kernel scale. For Isolation Forest, the main practical sequence is stability-first: increase ensemble size only if rankings are seed-fragile, and adjust \texttt{max\_samples} before threshold calibration. For LOF, neighborhood size \(k\) remains the primary sensitivity lever; larger \(k\) smooths noisy local decisions, while smaller \(k\) can recover finer local anomalies at the cost of variance.

DBSCAN should be tuned through a two-stage geometry-first process: adjust \(\epsilon\) around the knee-derived baseline to enter a plausible noise regime, then refine \texttt{min\_samples} to control cluster persistence. This ordering is more robust than searching both dimensions blindly. For ECOD and HBOS, score generation is comparatively stable and the operational risk is often in thresholding/calibration rather than representation learning. In practice, this means prioritizing contamination-policy sensitivity checks (ECOD) and discretization smoothness checks (HBOS) before attempting deeper model-level changes.

Across methods, a practical deployment pattern emerges: (i) tune one high-impact parameter axis at a time, (ii) monitor whether changes improve ranking consistency across seeds and nearby datasets rather than optimizing a single point estimate, and (iii) treat contamination estimates as operating-point initializers that require stress testing under plausible prevalence shifts. These recommendations align with the observed heterogeneity in Sections~\ref{sec:results} and the method-level sensitivity surfaces in Fig.~\ref{fig:results-sensitivity-missing-methods}.

Overall, the study supports a mixed strategy: robust global baselines for stable coverage, plus targeted local methods to recover geometry-specific anomalies.

\bibliographystyle{plain}
\bibliography{mybib}

\appendix

\section{Dataset Details}

This appendix reports corpus-level and per-dataset descriptive statistics used in the experimental design. Rows are computed from merged feature--label canonical tables before preprocessing (no robust scaling, no arrhythmia engineered matrix, no PCA exports). Missing fraction is defined as total pandas NA cells divided by \(n_{\text{rows}} \times n_{\text{cols}}\) of each canonical table (features plus label). As a result, appendix values can differ from older ``\% incomplete'' summaries: the ODDS-style \texttt{arrhythmia} table has no CSV-level NaNs (\(0.0000\)), although archival sources still report \(\sim\)0.33\% masked entries.

\subsection{Corpus-Level Summary}

Table~\ref{tab:dataset-summary} summarizes key corpus properties. The 37 datasets range from extremely small ($n=120$) to massive ($n=567{,}498$), and from low-d ($d=2$) to ultra-high-d ($d=400$), ensuring comprehensive coverage of failure modes.

\begin{table}[!ht]
\centering
\small
\caption{Corpus-level dataset summary. Statistics reflect raw datasets after canonicalization, before preprocessing.}
\label{tab:dataset-summary}
\begin{tabular}{lr}
\toprule
\textbf{Property} & \textbf{Value} \\
\midrule
Total datasets & 37 \\
ODDS datasets & 26 \\
2D benchmark datasets (Gagolewski) & 11 \\
Total observations & 1{,}086{,}545 \\
Observation count range & 120 -- 567{,}498 \\
Dimensionality range & 2 -- 400 \\
Contamination rate range & 0.0\% -- 35.9\% \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Per-Dataset Descriptive Statistics}

Table~\ref{tab:dataset-full} provides per-dataset statistics used to characterize solver stress and guide preprocessing decisions. Columns: \texttt{dataset\_id} (name), \texttt{n\_rows}/\texttt{n\_features} (size), \texttt{contam.} (anomaly rate), \texttt{missing} (fraction missing), \texttt{outliers}/\texttt{inliers} (counts), \texttt{mean $|\rho|$} (mean absolute correlation; 0=independent, 1=collinear), \texttt{CR} (distance concentration ratio; $\gg1$=intact geometry, $\lesssim0.1$=curse-of-dimensionality).

\begin{table}[!ht]
\scriptsize
\centering
\caption{Per-dataset descriptive statistics. No missing values were present in any dataset. CR stands for distance concentration ratio, $|\rho|$ for mean absolute correlation. Values rounded to 2 decimals.}\label{tab:dataset-full}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrrrrrrr}
\toprule
\textbf{dataset\_id} & \textbf{rows} & \textbf{cols} & \textbf{contam.} & \textbf{outliers} & \textbf{inliers} & \textbf{$|\rho|$} & \textbf{CR} \\
\midrule
annthyroid & 7200 & 6 & 0.07 & 534 & 6666 & 0.25 & 2.81 \\
arrhythmia & 452 & 274 & 0.15 & 66 & 386 & 0.09 & 2.46 \\
breastw & 683 & 9 & 0.35 & 239 & 444 & 0.60 & 2.28 \\
cardio & 1831 & 21 & 0.10 & 176 & 1655 & 0.22 & 2.95 \\
cover & 286048 & 10 & 0.01 & 2747 & 283301 & 0.19 & 2.52 \\
glass & 214 & 7 & 0.04 & 9 & 205 & 0.28 & 2.82 \\
http & 567498 & 3 & 0.00 & 2211 & 565287 & 0.06 & 4.21 \\
ionosphere & 351 & 32 & 0.36 & 126 & 225 & 0.24 & 1.96 \\
letter & 1600 & 32 & 0.06 & 100 & 1500 & 0.20 & 1.54 \\
lympho & 148 & 18 & 0.04 & 6 & 142 & 0.17 & 2.46 \\
mammography & 11183 & 6 & 0.02 & 260 & 10923 & 0.24 & 3.44 \\
mnist & 7603 & 100 & 0.09 & 700 & 6903 & 0.08 & 0.98 \\
musk & 3062 & 166 & 0.03 & 97 & 2965 & 0.29 & 1.38 \\
optdigits & 5216 & 64 & 0.03 & 150 & 5066 & 0.12 & 1.06 \\
pendigits & 6870 & 16 & 0.02 & 156 & 6714 & 0.26 & 1.39 \\
pima & 768 & 8 & 0.35 & 268 & 500 & 0.17 & 5.71 \\
satellite & 6435 & 36 & 0.32 & 2036 & 4399 & 0.50 & 3.09 \\
satimage-2 & 5803 & 36 & 0.01 & 71 & 5732 & 0.64 & 3.16 \\
shuttle & 49097 & 9 & 0.07 & 3511 & 45586 & 0.18 & 56.16 \\
smtp & 95156 & 3 & 0.00 & 30 & 95126 & 0.27 & 3.40 \\
speech & 3686 & 400 & 0.02 & 61 & 3625 & 0.03 & 0.59 \\
thyroid & 3772 & 6 & 0.02 & 93 & 3679 & 0.26 & 3.10 \\
vertebral & 240 & 6 & 0.13 & 30 & 210 & 0.39 & 6.72 \\
vowels & 1456 & 12 & 0.03 & 50 & 1406 & 0.27 & 1.78 \\
wbc & 223 & 9 & 0.04 & 10 & 213 & 0.42 & 3.97 \\
wine & 129 & 13 & 0.08 & 10 & 119 & 0.26 & 5.16 \\
wut/smile & 1000 & 2 & 0.00 & 0 & 1000 & 0.01 & 2.60 \\
wut/circles & 4000 & 2 & 0.00 & 0 & 4000 & 0.01 & 2.12 \\
wut/isolation & 9000 & 2 & 0.00 & 0 & 9000 & 0.01 & 1.90 \\
wut/windows & 2977 & 2 & 0.00 & 0 & 2977 & 0.03 & 2.11 \\
wut/x2 & 120 & 2 & 0.08 & 10 & 110 & 0.50 & 3.07 \\
sipu/compound & 399 & 2 & 0.00 & 0 & 399 & 0.32 & 2.40 \\
sipu/jain & 373 & 2 & 0.00 & 0 & 373 & 0.44 & 2.87 \\
sipu/flame & 240 & 2 & 0.05 & 12 & 228 & 0.02 & 2.11 \\
sipu/spiral & 312 & 2 & 0.00 & 0 & 312 & 0.00 & 2.24 \\
fcps/lsun & 400 & 2 & 0.00 & 0 & 400 & 0.10 & 2.38 \\
graves/fuzzyx & 1000 & 2 & 0.00 & 0 & 1000 & 0.01 & 2.92 \\
\bottomrule
\end{tabular}
\end{table}

\section{ODDS Outlier-Regime Taxonomy}

For tabular local-vs-global analysis in Section~\ref{sec:results}, ODDS datasets are grouped into Campos-style regime labels:
\begin{description}
    \item[Global/point-dominant] \texttt{annthyroid}, \texttt{cover}, \texttt{http}, \texttt{mammography}, \texttt{satimage-2}, \texttt{shuttle}, \texttt{smtp}, \texttt{thyroid}.
    \item[Local/cluster-dominant] \texttt{arrhythmia}, \texttt{breastw}, \texttt{cardio}, \texttt{glass}, \texttt{ionosphere}, \texttt{letter}, \texttt{lympho}, \texttt{mnist}, \texttt{musk}, \texttt{optdigits}, \texttt{pendigits}, \texttt{pima}, \texttt{satellite}, \texttt{speech}, \texttt{vertebral}, \texttt{vowels}, \texttt{wbc}, \texttt{wine}.
\end{description}

\section{2D Visual Assessment Gallery}

To satisfy broad visual assessment across the selected 2D corpus (11 Gagolewski datasets), this appendix provides all-model score maps for four representative and visually distinct cases: \texttt{wut/x2} (compact global anomalies, Fig.~\ref{fig:appendix-2d-gallery-x2}), \texttt{sipu/compound} (multi-cluster geometry, Fig.~\ref{fig:appendix-2d-gallery-compound}), \texttt{wut/smile} (curved manifold structure, Fig.~\ref{fig:appendix-2d-gallery-smile}), and \texttt{sipu/spiral} (strongly non-convex local topology, Fig.~\ref{fig:appendix-2d-gallery-spiral}). In each map, predicted anomalies are marked with crosses, so method-level false-positive spread and missed-cluster behavior can be inspected directly.

\begin{figure}[!ht]
\centering
\includegraphics[width=\columnwidth]{../outputs/figures/2d_compare_score_maps__wut__x2.png}
\caption{Appendix gallery: all-method 2D score-map panel for \texttt{wut/x2} with predicted anomaly markers (crosses).}
\label{fig:appendix-2d-gallery-x2}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\columnwidth]{../outputs/figures/2d_compare_score_maps__sipu__compound.png}
\caption{Appendix gallery: all-method 2D score-map panel for \texttt{sipu/compound} with predicted anomaly markers (crosses).}
\label{fig:appendix-2d-gallery-compound}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\columnwidth]{../outputs/figures/2d_compare_score_maps__wut__smile.png}
\caption{Appendix gallery: all-method 2D score-map panel for \texttt{wut/smile} with predicted anomaly markers (crosses).}
\label{fig:appendix-2d-gallery-smile}
\end{figure}

\begin{figure}[!ht]
\centering
\includegraphics[width=\columnwidth]{../outputs/figures/2d_compare_score_maps__sipu__spiral.png}
\caption{Appendix gallery: all-method 2D score-map panel for \texttt{sipu/spiral} with predicted anomaly markers (crosses).}
\label{fig:appendix-2d-gallery-spiral}
\end{figure}

\end{document}
